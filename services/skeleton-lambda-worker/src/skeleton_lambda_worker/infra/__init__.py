"""
Infrastructure layer - all I/O adapters (DynamoDB, S3, SQS, HTTP clients).

Add repository implementations and external-service clients here.
Use helpers from ``aws_utils`` (for example ``aws_utils.session``) for boto3
session management so credentials are handled consistently across services.
"""
