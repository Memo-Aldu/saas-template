"""
Domain layer - pure business entities and rules, no I/O.

Add Pydantic models or dataclasses here that represent the core concepts
of this worker (for example Job, Task, Event). Import shared enums/models
from ``domain_models`` as needed.
"""
